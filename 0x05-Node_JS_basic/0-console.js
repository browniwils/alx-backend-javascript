#!/usr/bin/node

// Script prints to standard output.

/**
 * Log message on stdout
 * @param {*} message
 */

const displayMessage = (message) => {
  console.log(message);
};

export default displayMessage;
module.exports = displayMessage;
